name: Setup Go
description: Set up Go. No caller permissions required.
inputs:
  github_token:
    description: Token to use for updating code with formatting fixes
    required: true
  go_version:
    description: The version of Go to use
    required: true
  install_ko:
    default: 'false'
    description: Whether to install Ko or not
    required: false
  install_task:
    default: 'false'
    description: Whether to install Task or not
    required: false

runs:
  using: composite
  steps:
    - name: Install Go
      uses: actions/setup-go@v6
      with:
        go-version: ${{ inputs.go_version }}

    - name: Configure Private Access
      run: git config --global url.https://${{ inputs.github_token }}@github.com/.insteadOf https://github.com/
      shell: bash

    - if: ${{ inputs.install_task == 'true' }}
      name: Install Task
      uses: go-task/setup-task@v2

    - if: ${{ inputs.install_ko == 'true' }}
      name: Install Ko
      uses: ko-build/setup-ko@v0.9
